{
   "type": "postgres",
   "host": "localhost",
   "port": 5432,
   "username": "postgres",
   "password": "your password",
   "database": "postgres",
   "synchronize": true,
   "logging": true,
   "__test__": [ "src/api/__test__/**/*.{js, ts}" ],   
   "controllers": [ "src/api/controllers/**/*.{js, ts}" ],
   "migration": [ "src/api/migration/**/*.{js, ts}" ], 
   "models": [ "src/api/models/**/*.{js, ts}" ],
   "repositories": [ "src/api/repositories/**/*.ts" ],
   "routes": [ "src/api/routes/*.{js, ts}" ],
   "cli": {
      "migrationDir": "src/api/migration"
   }
}
